 version: '2'
 services:
   db:
     image: imiell/sqlite
   web:
     build: .
     #command: python manage.py runserver 127.0.0.1:8000
     volumes:
       - /webapp:/code
     ports:
       - "8000:8000"
     depends_on:
       - db